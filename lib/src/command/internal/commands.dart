import '../model.dart';
/*
final internalCommands = <NodeCommand>[ping];

final ping = NodeCommand(
    name: "ping",
    executor: (NodeCommand cmd) async {
      cmd.payload = "pong";
      cmd.status = CommandStatus.success;
      return cmd;
    });
*/
